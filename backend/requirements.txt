# Flask and extensions
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-JWT-Extended==4.6.0
# 6.0.0 fixes CVE-2024-6221/6839/6844/1681 (inconsistent CORS origin matching,
# Access-Control-Allow-Private-Network default, log injection).
Flask-CORS==6.0.0
Flask-Limiter==3.5.0
Flask-Caching==2.1.0

# Database
psycopg2-binary>=2.9.11
redis>=5.0.0

# Validation
marshmallow==3.20.1

# Cryptography (optional field-level encryption at rest for stored API keys -
# active only when FIELD_ENCRYPTION_KEY is configured; see models/types.py).
cryptography>=42.0.0

# External APIs
openai>=1.50.0
httpx>=0.27.0
# 2.32.4 fixes CVE-2024-35195 (verify=False persisted across a Session) and
# CVE-2024-47081 (.netrc credential leak to arbitrary hosts).
requests==2.32.4

# Utilities
python-dotenv==1.0.0
APScheduler==3.10.4
# 3.0.6 fixes CVE-2024-49767 (multipart memory-exhaustion DoS) and
# CVE-2024-34069 (debugger RCE when the interactive debugger is exposed).
Werkzeug==3.0.6

# Production server
# 23.0.0 fixes CVE-2024-1135 (HTTP request smuggling via mishandled
# Transfer-Encoding, allowing proxy/WAF bypass and cache poisoning).
gunicorn==23.0.0

# Error monitoring (activated when SENTRY_DSN is set)
sentry-sdk[flask]==2.19.2

# Testing
pytest==7.4.3
pytest-cov==4.1.0
